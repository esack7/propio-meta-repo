---
name: prepare-spec
description: Validate repos.txt and create feature worktrees on feature/<spec-name> from origin/<default_branch>. Use when the user asks to prepare a spec, start feature worktrees, or run prepare-spec.
---

# Codex compatibility entrypoint

This directory is a discovery wrapper, not a second implementation.

Read and follow the canonical [prepare-spec skill](../../../.claude/skills/prepare-spec/SKILL.md) completely before acting. Resolve every helper and resource from that canonical skill directory; this wrapper intentionally contains no duplicated scripts.
