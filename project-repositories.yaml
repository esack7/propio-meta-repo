# Project map for cross-repo work. See AGENTS.md for schema guidance.
version: 1

repositories:
  - name: propio-agent
    description: Proprio agent implementation
    git:
      clone_url: https://github.com/esack7/propio-agent.git
      default_branch: main
    context:
      - README.md

  - name: propio-providers
    description: Proprio providers and integrations
    git:
      clone_url: https://github.com/esack7/propio-providers.git
      default_branch: main
    context:
      - README.md
